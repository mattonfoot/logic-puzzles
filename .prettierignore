node_modules
dist
.expo
.screenshot-build
docs/screenshots
package-lock.json

# Markdown is left alone: Prettier pads table cells out to the widest row, and
# the screenshot table in the README would become unreadable in source.
*.md

# Generated by `npm run icons` from the SVG files in assets/icons. Prettier
# would reflow the path data onto its own lines and the next run would put it
# back, so the two are kept out of each other's way.
src/ui/icons.generated.ts
docs/icons.html
